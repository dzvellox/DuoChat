{
  "name": "duochat-extension",
  "version": "1.3.1",
  "private": true,
  "description": "Extension Chrome locale multi-profils pour ChatGPT et Claude.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check core.js && node --check background.js && node --check content.js && node --check popup.js && npm test"
  }
}
